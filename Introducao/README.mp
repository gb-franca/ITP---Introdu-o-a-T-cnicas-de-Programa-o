# Tópico 01: Introdução à Linguagem C

Esta pasta contém as soluções para os problemas da seção "Iniciante" do Beecrowd, que correspondem ao primeiro tópico da disciplina de **Introdução à Técnica de Programação (ITP)**.

## 💡 Conceitos Abordados

Nesta fase inicial, o foco foi nos fundamentos da linguagem C. Os principais conceitos praticados estão detalhados abaixo.

### 1. Estrutura Básica de um Programa
A função `main()`, a inclusão de bibliotecas com `#include` (como a `<stdio.h>`) e a instrução `return 0;` para indicar que o programa terminou com sucesso.

### 2. Variáveis e Tipos de Dados Primitivos
* **`int`**: Para armazenar números inteiros.
* **`double`** e **`float`**: Para armazenar números de ponto flutuante (decimais), sendo `double` o tipo com maior precisão e o mais exigido nos problemas.
* **`char`**: Para armazenar um único caractere.
* **`const`**: Para definir valores que não podem ser alterados, como `const double PI = 3.14159;`.

### 3. Entrada, Saída e Especificadores de Formato
A manipulação de entrada (`scanf`) e saída (`printf`) é a base para a comunicação com o usuário e para apresentar os resultados no formato exigido pelo Beecrowd. Para isso, utilizamos os **especificadores de formato**:

| Especificador | Tipo de Dado     | Uso Principal e Observações                                      |
| :---:         | :---             | :---                                                             |
| **`%d` ou `%i`** | `int`            | Lê ou imprime um número inteiro decimal.                         |
| **`%f`** | `float`, `double`| Usado no `printf` para imprimir números de ponto flutuante.        |
| **`%lf`** | `double`         | **Essencial** no `scanf` para ler corretamente uma variável `double`. |
| **`%c`** | `char`           | Para ler ou imprimir um único caractere.                           |
| **`%s`** | `char[]`         | Para ler ou imprimir uma string (sequência de caracteres).         |

**Formatando a Saída:** É muito comum precisar formatar o número de casas decimais. Para isso, usa-se o modificador `.` (ponto).
* **Exemplo:** `printf("MEDIA = %.5lf\n", media);` imprimirá a variável `media` com exatamente **5 casas decimais**.

## 🎯 Desafios
Os problemas nesta seção foram essenciais para me familiarizar com a sintaxe da linguagem C e entender como resolver problemas simples que envolvem cálculos matemáticos e a manipulação de entrada e saída de dados, que são a base para tópicos mais complexos.

[↩️ Voltar para o README principal](../README.md)
